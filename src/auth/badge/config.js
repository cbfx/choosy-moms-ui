import config from './../config';

export default {
  ID: `${config.ID}-badge`,
  COMPONENT_NAME: `${config.ID}Badge`,
  COMPONENT_TAG: `<${config.ID}-badge></${config.ID}-badge>`,
  NAMESPACE: `${config.NAMESPACE}.badge`,
  TITLE: `${config.TITLE} Badge`
};
